# Active Tracks

## ane_honesty_fail_closed

- status: closed
- owner: conductor
- intent: course-correct the ANE training shell so explicit ANE requests fail closed instead of sliding into wrapper theater or silent regular-CPU continuation
- bounded_slice: CLI and training-shell handling for explicit ANE execution requests
- product_corpus:
  - `Sources/Autotrade/GraphShowdown.swift`
  - `Tests/AutotradeTests/IntegrationTests.swift`
- acceptance:
  - explicit ANE-required mode exists
  - explicit ANE-required mode exits with a concrete failure when ANE is unavailable or produces zero updates
  - normal CPU path remains available without ANE overhead when ANE is not required
  - focused integration tests cover the new contract
- verification:
  - command: `swift test --filter IntegrationTests`
  - result: passed on 2026-03-26

## ane_proof_surface_fail_closed

- status: closed
- owner: conductor
- intent: make ANE execution self-proving so explicit ANE-required runs fail closed unless they can demonstrate real ANE work instead of wrapper mode only
- bounded_slice: aggregate ANE proof counters in the wrapper and shell, print them, and enforce them for required ANE runs
- product_corpus:
  - `Sources/GraphShowdownANE/GraphShowdownANE.swift`
  - `Sources/Autotrade/GraphShowdown.swift`
  - `Tests/AutotradeTests/IntegrationTests.swift`
- acceptance:
  - ANE wrapper exposes proof data beyond `backendMode`
  - training shell prints an ANE proof summary for ANE runs
  - explicit `--ane-required` fails closed when proof counters show zero ANE work
  - focused integration tests cover proof-allowed and proof-forbidden behavior
- verification:
  - command: `swift test --filter IntegrationTests`
  - result: passed on 2026-03-26

## silicon_lane_reference_lane_unbias

- status: open
- owner: conductor
- intent: isolate the direct-silicon execution seam from the external reference lane so later implementation can harden both without cross-contaminating assumptions
- bounded_slice: repo-local cell decomposition for autotrade's direct runtime wrapper surfaces and the vendored reference-comparison surfaces
- product_corpus:
  - `Sources/GraphShowdownANE/GraphShowdownANE.swift`
  - `Sources/AutotradeHRM/HRMANETrainer.swift`
  - `museum/ane/Espresso/Sources/Espresso/MultitokenProbeSupport.swift`
  - `museum/ane/Espresso/Sources/EspressoMultiTokenProbe/main.swift`
  - `museum/ane/Espresso/Sources/EspressoGenerate/CLI.swift`
- acceptance:
  - exactly two isolated cells are named with disjoint corpora
  - each cell is expressed as a functional output contract without tutorial or lineage phrasing
  - the direct execution seam and the external reference lane do not share execution context before lockdown
  - a fresh later `implement` turn can perform LOCKDOWN across both cells
- evidence:
  - `Sources/GraphShowdownANE/GraphShowdownANE.swift` is the autotrade runtime wrapper that imports `Espresso` and owns the ANE proof surface
  - `Sources/AutotradeHRM/HRMANETrainer.swift` is the autotrade adapter stub that still promises checkpoint import/export and weight updates for the same runtime lane
  - `museum/ane/Espresso/Sources/Espresso/MultitokenProbeSupport.swift` owns the vendored `CoreML` request model and reference execution path
  - `museum/ane/Espresso/Sources/EspressoMultiTokenProbe/main.swift` and `museum/ane/Espresso/Sources/EspressoGenerate/CLI.swift` are the command surfaces that expose the reference lane
- cells:
  - `glass_forge`:
    - corpus:
      - `Sources/GraphShowdownANE/GraphShowdownANE.swift`
      - `Sources/AutotradeHRM/HRMANETrainer.swift`
    - produces: a fail-closed direct execution contract that names real buffer flow, proof emission, checkpoint boundaries, and removal of placeholder behavior
    - stop_condition: one bounded slice or first blocker inside the direct execution seam
    - verification_surface:
      - `swift test --filter IntegrationTests`
    - isolation_rule: no reads or edits under `museum/ane/Espresso/`
  - `mirror_harbor`:
    - corpus:
      - `museum/ane/Espresso/Sources/Espresso/MultitokenProbeSupport.swift`
      - `museum/ane/Espresso/Sources/EspressoMultiTokenProbe/main.swift`
      - `museum/ane/Espresso/Sources/EspressoGenerate/CLI.swift`
    - produces: a reference-lane contract that makes external comparison inputs, compute-unit policy, and report surfaces explicit without reaching into autotrade runtime code
    - stop_condition: one bounded slice or first blocker inside the vendored reference lane
    - verification_surface:
      - `swift test --package-path museum/ane/Espresso --filter MultitokenProbeSupportTests`
    - isolation_rule: no reads or edits under `Sources/GraphShowdownANE/` or `Sources/AutotradeHRM/`
- lockdown:
  - trigger: fresh conductor `implement` session after both cells report closed
  - full_context_required: true
  - verification_goal: confirm the direct execution seam and the reference lane meet at explicit interfaces only, with no hidden fallback or benchmark coupling
- current_slice:
  - cell: `glass_forge`
  - status: verified
  - objective: make checkpoint persistence explicit and fail-closed by reconciling `ANEModelWrapper.save/load` with the trainer's checkpoint-unavailable contract without weakening the existing ANE proof surface
  - corpus:
    - `Sources/AutotradeHRM/HRMModel.swift`
    - `Sources/GraphShowdownANE/GraphShowdownANE.swift`
    - `Sources/Autotrade/GraphShowdown.swift`
    - `Tests/AutotradeTests/IntegrationTests.swift`
    - `Tests/AutotradeTests/HRMModelTests.swift`
  - acceptance:
    - checkpoint operations surface concrete failures instead of silent no-op or partial-snapshot behavior
    - the ANE wrapper no longer claims active-ANE checkpoint persistence that the trainer cannot honor
    - CPU HRM checkpoint round-trip remains available
    - focused tests pin ANE checkpoint failure and CPU checkpoint round-trip behavior
  - verification:
    - `swift test --filter IntegrationTests`
    - `swift test --filter HRMModelTests`
    - result: passed on 2026-03-26
  - delivered:
    - `HRMEdgeBackend.save/load` now throw explicit checkpoint errors instead of silently swallowing failure
    - `ANEModelWrapper.save/load` now fail closed in active ANE mode and only delegate to `HRMModel` checkpoint I/O in CPU fallback mode
    - `GraphShowdown.swift` routes CPU checkpoint reads and writes through guarded helpers that tolerate missing files but surface real checkpoint errors
    - `IntegrationTests` and `HRMModelTests` pin the CPU checkpoint round-trip path and the live-ANE fail-closed boundary
  - next_slice:
    - cell: `mirror_harbor`
    - status: in_progress
    - objective: promote the multitoken probe reference lane from ad hoc payload assembly to an explicit typed plan/report seam that names comparison inputs and compute-unit policy without reading or editing autotrade runtime code
    - corpus:
      - `museum/ane/Espresso/Sources/Espresso/MultitokenProbeSupport.swift`
      - `museum/ane/Espresso/Sources/EspressoMultiTokenProbe/main.swift`
      - `museum/ane/Espresso/Tests/EspressoTests/MultitokenProbeSupportTests.swift`
    - acceptance:
      - the validated multitoken probe plan exposes comparison input identity explicitly
      - compute-unit policy is carried through an explicit typed seam instead of only ad hoc payload keys
      - compare and compile payload assembly consume that typed seam
      - focused `MultitokenProbeSupportTests` pin the plan/report contract
    - verification_surface:
      - `swift test --package-path museum/ane/Espresso --filter MultitokenProbeSupportTests`
    - runtime_route:
      - `spawn_agent worker via host default runtime (no repo-local model override)`
    - stop_condition:
      - `one bounded slice or first blocker inside the multitoken probe reference seam`
