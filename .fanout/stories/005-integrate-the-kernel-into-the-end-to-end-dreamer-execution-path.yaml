acceptance:
- The targeted Dreamer execution path consumes the WASM-backed results end to end
  without bypassing the adapter for the promoted computations
- State updates, rolling windows, and batched feature production remain consistent
  across repeated ticks/runs with the new integration
- Feature flags or rollout controls allow the promoted WASM path to be enabled, verified,
  and reverted cleanly
dependencies:
- '004'
description: Replace the existing precision-sensitive computation path with the new
  adapter inside the real Dreamer flow. Wire batching, scheduling, history/ring-buffer
  updates, and result consumption so the WASM path is the delivered path rather than
  an isolated module.
id: '005'
name: Integrate the kernel into the end-to-end Dreamer execution path
